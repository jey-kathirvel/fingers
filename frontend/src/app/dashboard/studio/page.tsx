import PlaceholderPage from "@/components/placeholder";

export default function Page() {
  return (
    <PlaceholderPage
      title="AI Studio"
      body="Phase 2 will generate platform-specific hooks, captions, scripts and variants from brand context."
    />
  );
}
